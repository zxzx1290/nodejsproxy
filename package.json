{
  "name": "nodejsproxy",
  "version": "1.0.0",
  "description": "",
  "main": "app.js",
  "scripts": {
    "start": "NODE_ENV=prod node app.js",
    "test": "NODE_ENV=test node_modules/.bin/istanbul cover node_modules/.bin/_mocha && cat ./coverage/lcov.info | coveralls && rm -rf ./coverage"
  },
  "author": "",
  "dependencies": {
    "cookie": "^0.3.1",
    "http-proxy": "^1.17.0",
    "querystring": "^0.2.0",
    "redis": "^2.8.0",
    "request": "^2.88.0",
    "speakeasy": "^2.0.0"
  },
  "devDependencies": {
    "coveralls": "^3.0.4",
    "expect.js": "^0.3.1",
    "istanbul": "^0.4.5",
    "mocha": "^5.2.0",
    "mocha-lcov-reporter": "^1.3.0"
  }
}
